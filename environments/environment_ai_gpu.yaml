name: bayesevt_ai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  # - conda-forge::jupytext
  # - conda-forge::jupyter-book
  # - conda-forge::myst-parser
  # - conda-forge::pre-commit
  # - conda-forge::"nodejs>=20,<21"
  # - conda-forge::mystmd
  - conda-forge::iris
  - conda-forge::iris-grib
  - cudatoolkit
  - xarray
  - cartopy
  - cmocean
  - seaborn
  - matplotlib
  - xmovie
  - ipykernel
  - ffmpeg
  - pip
  - pip:
      - cookiecutter
      - autoroot
      # DATA ACCESS
      - climetlab
      - xee
      - ecmwf-api-client
      - cdsapi
      # VISUALIZATION
      # - lexcube
      - tqdm
      # ECMWF MODELS
      - ai-models
      # - ai-models-panguweather
      # - ai-models-graphcast
      # - ai-models-fourcastnet
      - ai-models-fourcastnetv2
      - git+https://github.com/NVIDIA/earth2mip.git
      - git+https://github.com/NVIDIA/apex.git -v --disable-pip-version-check --no-cache-dir --no-build-isolation --global-option="--cpp_ext" --global-option="--cuda_ext"
      # - git+https://github.com/deepmind/graphcast.git
    #   - sphinx-proof
      - loguru 
